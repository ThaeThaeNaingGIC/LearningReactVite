import { react } from "react";

function MainContent() {
  return (
    <div>
      <h1>Vite + React</h1>
      <ol className="mx-5">
        <li>
          React Native allows developers to create mobile apps using website
          technology.
        </li>
        <li>
          React Native allows developers to build cross-platform apps that look
          and feel entirely Native since it uses JavaScript components that are
          both built on iOS and Android components.
        </li>
      </ol>
    </div>
  );
}

export default MainContent;
